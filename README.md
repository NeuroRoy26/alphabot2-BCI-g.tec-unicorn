# README

This documentation provides the necessary instructions to run the project.

To run the program, use the following commands:

```bash
python ssvep_generator.py
```

Make sure to have the appropriate files in place:
- ssvep_gen_MATLAB.m is required for MATLAB usage.

**Note:** The file `record_realtime_model.slx` is not included in the repository.